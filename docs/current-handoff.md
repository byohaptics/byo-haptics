# Current Handoff

Updated: 2026-08-21

## Current State

The history-free public repository foundation exists locally with BYO Haptics at version `0.2.0`. Public architecture, Output Plugin contract, package lifecycle, device plugin behavior, SlotSpec schema, host UI, lifecycle, sampler behavior, development loop, tasks, implementation map, test plan, known limitations, and live-result ledger have initial specifications. `versions.json` records product, document, contract, plugin, Bridge, and Bridge API versions. The SlotSpec builder requires generated IDs and has no runtime-ID fallback. All six host sheets, both Plugin Package sheets, and shared transform helpers compile with no unresolved modules. Deployment scripts pass F# type checking and require generated IDs plus an explicit deployment gate. Joy-Con and Haptira packages use public IDs, contract v1, version 0.1.1, and validated defaults. The self-contained Joy-Con Bridge application compiles at version `0.1.0`; all Bridge tests and strict Clippy checks pass. Windows CI installs the pinned FluxSDK and runs the complete static suite. User packaging produces one application with build paths remapped and rejects remaining private paths; its background Bridge mode runs from that same binary. Private history, runtime logs, traces, generated scene artifacts, local paths, and real Bluetooth addresses were not migrated. The publication-policy scan and Git history scan are clean.

Third-party plugin authoring is documented in `docs/plugin-authoring.md`. The tracked 10-slot, 21-component minimal template provides a contract-v1 package card and a ProtoGraph that routes Target `default` Force from all four rows to an inspectable field, clears output while inactive, and reports connection state as unknown. Its SlotSpec passes the standard validator as part of `npm test`, and its graph compiles with zero module-discovery errors as part of `npm run compile:plugins`. Third-party License values and versions remain author-owned rather than inheriting the built-in `byohaptics` policy.

The public build was redeployed to `byohaptics World` under Resonite user `byohaptics` on 2026-08-13 after local Resonite data loss. World Root contains one public `BYOHaptics` Host, one Joy-Con OSC Plugin Package, and one Haptira OSC Plugin Package. The Host deployed eight sheets and each plugin deployed one sheet with zero module-discovery errors. Install, uninstall, panel grab, both close behaviors, context-menu and UI panel reset, sampler offset editing, and Reset All Positions passed live tests.

Joy-Con Bridge and Plugin defaults now use `9010/UDP` to avoid common face-tracking ports. Bridge controller bindings default to `bluetooth_address = "auto"`, which selects the first connected controller of each configured side; an explicit address remains available for disambiguation. Live Force tests passed end to end: Row 0/Target `left` drove Joy-Con (L), and Row 1/Target `right` drove Joy-Con (R).

`docs/joycon-bridge-contract.md` is now the source of truth for the versioned Joy-Con Plugin-to-Bridge boundary. Bridge API `0.1.0` fixes the existing OSC paths, argument types, Target rules, acknowledgement registration, 500 ms heartbeat, 2000 ms Bridge timeout, final-stop guarantee, and compatibility policy without changing runtime behavior. The application and Plugin documents link to this contract rather than defining duplicate wire details.

The Joy-Con Bridge GUI now presents the left and right controller state, vibration service state, and BYO Haptics connection state separately. A filled green `●`, red `×`, and gray `－` encode positive, negative, and unchecked state without relying on color alone or a thin hollow circle. Running-controller labels use the Bridge's live connection state; stopped-controller labels explicitly describe the last manual check as `検出済み` or `未検出`. Starting the service invalidates that snapshot, so an immediate failure cannot leave a stale `接続` indication, and the missing-controller error is localized in the GUI details. The user GUI no longer exposes file names, storage locations, communication addresses, OSC terminology, or a second executable. One self-contained application runs the controls, device scan, measurement, and background vibration mode; mutable data is stored automatically in the Windows user's local application-data area. The generated single application passed a background-mode UDP bind check and is running for live use. Its window, taskbar entry, executable, and shortcuts use the repository-owned BYO Haptics artwork, with the original SVG retained beside the generated icon assets. Starting the Bridge requires both configured controllers. HID read or write failure terminates the service rather than retaining a nonfunctional process or attempting an unreliable background reconnect. Hardware-disconnect behavior and the three live indicators still require live verification.

The Joy-Con Plugin Package card exposes configurable Bridge Address and Port fields, defaulting to `127.0.0.1:9010`. The address is passed directly to Resonite's OSC sender without host-name resolution or rewriting. The acknowledgement receiver remains on its separate port. Live device output passed for Force, Vibration, and Pain.

Haptira OSC live tests passed for Targets `00`, `01`, `02`, and upper channel `15`. In a two-user session, both Joy-Con OSC and Haptira OSC drove their devices from Row 0 for both users.

Haptira now resends all 16 current channel values every 50 ms while any output is active and for 250 ms after the last positive output. The bounded tail repeats final zero values to tolerate UDP loss without enabling permanent sender-wide resend. The package was rebuilt and redeployed to `byohaptics World` with zero module-discovery errors on 2026-08-17. Repeated GunRecoil verification remains pending.

Node source mode passed with Row 3 following BodyNode `RightFoot` and driving Haptira Target `01`. Slot source mode passed for Rows 0 through 3 with assigned Source slots. Null-source disable passed for all four rows with Node set to `NONE`, and Slot mode remained inactive with Source null. Plugin replacement automatically installed the dropped plugin and ejected the previous plugin. Inventory save, spawning and installation in another world passed. Installation and device output also passed in another user's world. Stopping the Joy-Con Bridge changed the Link indicator to down; restarting the Bridge restored both the connected indication and device output without reinsertion. Phase 6 live verification is complete. The private GitHub repository exists under `byohaptics`, with `main` and `v0.1.0` pushed.

Both Plugin Package SlotSpecs now include the same `License` policy as the Host: credit required, credit string `byohaptics`, and export disabled. The source change requires rebuilding and redeploying both Plugin Packages before the live objects contain these components.

The Demo Output Plugin is implemented at version `0.1.6`. The user passed direct drop, two-row `left`/`right` routing, HapticVolume response, and visible motion in `byohaptics World`. The Wiggler targets the dedicated `Devices/<side>/Visual.Rotation` child field, eliminating the display-writer conflict while retaining the thirty-degree maximum. Its card remains content-sized at 420 by 186 pixels, and the device roots remain at x = -0.55 m and +0.55 m. The individual device `Grabbable` components were removed after the Right device was accidentally released to World Root; only the complete package remains grabbable, preserving package-owned direct references. The corrected 26-slot, 56-component package was rebuilt and most recently redeployed to `byohaptics World` on 2026-08-18 with a 408-response graph and zero module-discovery errors. Live readback confirmed both devices remain under the package Root, the package is grabbable and licensed, neither device is independently grabbable, and the output graph is active; the user also previously confirmed by direct interaction that Right cannot be grabbed.

Sampler Edit uses stable labels `0` through `3`, always exposes all four rows, and each Host UI row starts with `0:` through `3:`. The user confirmed the row-index position, all four Sampler Edit items, and Row 0 selection behavior. The row-index font is now 32 px in a fixed 60 px column, and the header has a matching blank 60 px column so Node and later headers align with their controls. The updated Host was rebuilt and redeployed to `byohaptics World`; live inspection confirmed the new width and font values. Visual acceptance of the enlarged index and checkbox clearance is pending.

The Output Bus retains each positive Force, Vibration, Pain, and Temperature value independently per row for at least `Config/MinimumPulseDuration`. The default is `0.08` seconds, zero disables retention, a new positive sample replaces the retained value and restarts its duration, and an invalid Host, row, Source, or Target still clears output immediately. The user confirmed in VR that short collisions feel effective. The 16 scalar hold fields are now four row-local `float4` fields whose lanes remain independent, and the existing `LocalUpdate` decrements them with `DeltaTime` instead of a separate 10 ms `SecondsTimer`. Fixed Host components fell from 662 to 650, Output Bus responses from 1429 to 1048, and the live total from 1568 to 1403 slots. All static tests and Host graph compilation pass. The vectorized Host was deployed to `byohaptics World` on 2026-08-18; live readback confirmed all six sheets active, `MinimumPulseDuration = 0.08`, and zeroed independent row hold vectors. Perceptual confirmation after this vectorized deployment remains pending.

The user subsequently confirmed manual Slot source mode on the deployed vectorized Host. A further source-only compaction groups internal Config state, all Output Bus contract components, and plugin indicator fields by ownership, removes nineteen duplicate full-stretch UI wrapper Slots, and deletes unused Host configuration/version fields plus constant Sampler Edit visibility copies. The Host SlotSpec fell from 245 to 174 declared Slots and from 650 to 597 components. The deploy script also no longer scans/removes graphs from the fresh Host, arranges ModuPrint sheets, or reconciles a deployment-only Installed value. The compacted Host was rebuilt and deployed to `byohaptics World` on 2026-08-18 with all six sheets, zero module-discovery errors, zero input-reference corrections, and 1331 live slots. Live readback confirmed `GlobalGain = 1`, `MinimumPulseDuration = 0.08`, and the expected uninstalled World Root state. The user then confirmed Node source mode drives the installed Demo Output Plugin correctly. Visual inspection and the remaining Slot-mode, clear, offset, plugin-status, and pulse-retention regressions remain pending.

A repository-wide Ponytail audit removed 416 net lines. Host deployment now has one path: `deploy-host.ps1` builds a fresh SlotSpec, then all current sheets consume only that build's generated IDs. The unused live-graph discovery, stale-ID recovery, diagnostic-field repair, partial-sheet, patch-only, and keep-previous paths are gone. The Bridge now stores its concrete deterministic PRNG directly instead of carrying a test-only random-source abstraction, and the Plugin wrapper no longer sets an unreachable fallback URL after SlotSpec construction. All static checks and 33 Rust tests pass.

Host lightweight work removed the read-only Diagnostics sheet and merged Source Binding clear behavior into Sampler. Fixed SlotSpec structure fell from 294 to 245 slots, and the measured live total fell from 2088 to 1588 slots, a reduction of 500 (23.9%). The Host now deploys six sheets. Automatic BodyNode-to-Target suggestion was removed; explicit Target routing remains. The lightweight Host was deployed to the `Local` session with zero module-discovery errors. Core live behavior requires regression testing.

The first lightweight Host regression left Source tracking, Debug Visuals, and Demo output inactive. Live probing isolated the failure to the Sampler ProtoFlux `LocalUpdate` sequence: both operation references were null. The preceding Source-clear implementation synthesized four `BodyNode.NONE` and four null Slot constant paths that ResoniteLink could not add; its failed batch reference resolution also left the later LocalUpdate sequence unwired. Source clearing now uses native `ButtonValueSet<BodyNode>` and `ButtonReferenceSet<Slot>` components, and the unsupported ProtoFlux writes are gone. `ValueUserOverride<bool>` remains on the always-active Host root and directly drives `Samplers.IsActive`; the speculative intermediate `ValueField<bool>` and `ValueCopy<bool>` were removed. The corrected Host deployed six sheets without ResoniteLink cast or unresolved-reference exceptions, and live inspection confirmed both Sampler LocalUpdate calls were wired. After installation, Rows 0 and 1 followed `LeftHand` and `RightHand`, both Debug Visuals appeared, and both simulated devices reacted to a Force Emitter. The regression is closed.

## Next Task

Verify Joy-Con disconnect handling and all three Bridge GUI status indicators on Windows.

## Blocking Decisions

None.
